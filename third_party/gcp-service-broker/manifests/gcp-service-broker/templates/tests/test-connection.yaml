---
# Source: gcp-service-broker/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "gcp-service-broker-test-connection"
  labels:
    app.kubernetes.io/name: gcp-service-broker
    helm.sh/chart: gcp-service-broker-5.0.0
    app.kubernetes.io/instance: gcp-service-broker
    app.kubernetes.io/managed-by: Tiller
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['gcp-service-broker:80']
  restartPolicy: Never
